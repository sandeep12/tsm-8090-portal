/** Placeholder until WO-5 implements the sign-in flow. */
export function SignInPlaceholder() {
  return (
    <div className="page page--centered">
      <h1>Sign in</h1>
      <p>Sign-in screen and authentication flow arrive in a later work order.</p>
    </div>
  );
}
