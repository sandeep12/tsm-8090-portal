# MongoDB connection string (Atlas or local)
MONGODB_URI=mongodb://127.0.0.1:27017/tsm-portal

# HTTP server
PORT=3000

# Auth (required to boot the server; used by WO-4)
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=8h
